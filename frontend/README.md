# Wishora Frontend (Vite + React)

## Scripts

```bash
npm run dev
npm run build
npm run preview
```

## Environment

- `VITE_API_URL` (optional): backend API base URL (defaults to `http://localhost:5000/api`)

